import { requireAdminSession } from "../middleware/admin-session.js";
import { Hono } from "hono";import { z } from "zod";import { getCustomerHistory,getVehicleHistory,listCustomersWithHistory,listVehiclesWithHistory } from "../repositories/history.js";export const historyRoutes=new Hono();historyRoutes.use("*",requireAdminSession);const id=z.string().uuid();historyRoutes.get("/customers",async c=>c.json({customers:await listCustomersWithHistory()}));historyRoutes.get("/customers/:id",async c=>{const p=id.safeParse(c.req.param("id"));if(!p.success)return c.json({error:"invalid_customer_id"},400);const x=await getCustomerHistory(p.data);return x?c.json(x):c.json({error:"customer_not_found"},404)});historyRoutes.get("/vehicles",async c=>c.json({vehicles:await listVehiclesWithHistory()}));historyRoutes.get("/vehicles/:id",async c=>{const p=id.safeParse(c.req.param("id"));if(!p.success)return c.json({error:"invalid_vehicle_id"},400);const x=await getVehicleHistory(p.data);return x?c.json(x):c.json({error:"vehicle_not_found"},404)});